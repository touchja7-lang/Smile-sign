{
  "name": "smile-sign-app",
  "version": "1.0.0",
  "description": "Smile Sign - Full Stack App",
  "main": "backend/server.js",
  "scripts": {
    "postinstall": "cd backend && npm install && cd ../smile-sign && npm install && npm run build",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
